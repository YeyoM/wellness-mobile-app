// This function will take n objects, in which each object represent a workout,
// In each workout we will have a bunch of fields, but we are interested
// in the "date" field, which will be used to filter the data and the "totalTime"
//
// The function will return an array of objects, in which each object will have the following fields:
// - date: the date of the workout
// - totalTime: the total time spent in the workout
//
// This output array should also have days even if there are no workouts for that day
// (in which case the totalTime will be 0)
export default async function getUserTimeSpentProgressDataForGraph({
  workouts,
}) {}
